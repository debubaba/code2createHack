from django.db import models

# Create your models here.
# class Duststats(models.Model):
#     prevDegradable = models.BigIntegerField(default=0)
#     prevNonDegradable = models.BigIntegerField(default=0)
#     prevRecycleable = models.BigIntegerField(default=0)
#     prevNonRecycleable = models.BigIntegerField(default=0)

#     currDegradable = models.BigIntegerField(default=0)
#     currNonDegradable = models.BigIntegerField(default=0)
#     currRecycleable = models.BigIntegerField(default=0)
#     currNonRecycleable = models.BigIntegerField(default=0)
